import React from "react";
import { EnterpriseDiscoveryWorkspace } from "./EnterpriseDiscoveryWorkspace";

export const DiscoveryPage: React.FC = () => {
  return <EnterpriseDiscoveryWorkspace />;
};

export default DiscoveryPage;
